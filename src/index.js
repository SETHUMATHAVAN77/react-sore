import React from "react";
import ReactDOM from "react-dom/client";

import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// jsx

// components
// props-properties
// object -key value should be same

// convert the data into object

// lists in react
// list is an another name for array
// inside return write js u should put {}
// it will take the all the elements in the array and render them

// list with  map method

// map-array forEach -undefined
//  we want the final output in array format

// map will take the array and modifies the array and return the array

// we will spread at the data passing stage

// react wants every element having unique properties

// react render one one item separately

// if your doing crud operations in list  we use add property key

// key -to create a unique number

// everyTime u create a list u should have a key

// IMPORT AND EXPORT -we use to pass information from one js to other

// TWO types of exports
// default export - only one default export can be there in a js file

// named exports - you can pass any number of variables as named exports

// you combine both

// export const userName = "Sethu"; Named export

// export default "Mathavan";

// export default function sum(a, b){return a + b}

// in a single module all the named export will come as a object put{}

// in default export to import u can give any values

// first to data its api and what type and its not api let do with the array of object type.
